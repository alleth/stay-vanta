# API origin for the StayVanta backend.
# Leave unset in local dev — Vite proxies /api to http://localhost:8765
# (see vite.config.js). In production (Cloudflare Pages) set this to the
# deployed CakePHP API origin, e.g. https://api.stayvanta.com
VITE_API_BASE_URL=
