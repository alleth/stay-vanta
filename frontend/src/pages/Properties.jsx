import ModuleStub from '../components/ModuleStub'

export default function Properties() {
  return (
    <ModuleStub
      title="Properties"
      subtitle="Hotels & resorts (owner only)."
      features={[
        'Add a Hotel or Resort',
        'Assign an Admin user to each property',
        'Platform-wide oversight',
      ]}
    />
  )
}
