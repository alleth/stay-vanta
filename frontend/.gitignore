# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# package-lock.json is intentionally not committed: a Windows-generated
# lock omits Linux-only optional deps and breaks Cloudflare (Linux) `npm ci`.
# Cloudflare runs `npm install` without it, which resolves correctly.
package-lock.json
